services:
  db:
    image: postgres:15-alpine
    restart: unless-stopped
    environment:
      POSTGRES_USER: mobilefrost_user
      POSTGRES_PASSWORD: secretpassword
      POSTGRES_DB: mobilefrost_db
    volumes:
      - pgdata:/var/lib/postgresql/data

  controller:
    build: .
    restart: unless-stopped
    depends_on:
      - db
    devices:
      - "/dev/arduino_sensor_marten:/dev/arduino_sensor_marten"
      - "/dev/arduino_sensor_andor:/dev/arduino_sensor_andor"
      - "/dev/arduino_sensor_luis:/dev/arduino_sensor_luis"
      - "/dev/arduino_aktor:/dev/arduino_aktor"
    environment:
      - DB_HOST=db
      - DB_PORT=5432
      - DB_USER=mobilefrost_user
      - DB_PASSWORD=secretpassword
      - DB_NAME=mobilefrost_db

volumes:
  pgdata: